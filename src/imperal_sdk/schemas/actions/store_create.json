{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://imperal.io/schemas/actions/store_create.json",
  "title": "store.create step args",
  "description": "Args for the 'store.create' declarative step verb — insert a new record.",
  "type": "object",
  "required": ["kind", "data"],
  "properties": {
    "kind": {"type": "string", "description": "Data store entity kind (collection name)"},
    "data": {"type": "object", "description": "Field values for the new record (the interpreter's run_store reads args['data'])"}
  },
  "additionalProperties": true
}
