"""Sprint 1.1 config-store HTTP tests — OBSOLETE after Sprint 1.2.

The _load_config_store and _resolve methods were stripped in Sprint 1.2
(kernel becomes the sole resolver; SDK reads from ctx._llm_configs).

These tests are deleted rather than refactored because the contract
they tested no longer exists. Sprint 1.2 ctx-injection coverage lives
in test_llm_provider_ctx_injection.py.
"""
# All tests removed in Sprint 1.2.
